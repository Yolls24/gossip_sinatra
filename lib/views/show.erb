<h1>Potin de <%= gossip.author %></h1>
<p class="gossip-content"><%= gossip.content %></p>

<div class="actions">
  <a href="/" class="btn">🏠 Accueil</a>
  <a href="/gossips/<%= gossip.id %>/edit" class="btn">✏️ Éditer</a>

  <form action="/gossips/<%= gossip.id %>/delete" method="post" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce potin ?');">
    <button type="submit" class="btn delete-btn">🗑️ Supprimer</button>
  </form>
</div>

<hr>

<h2>💬 Commentaires</h2>

<% if comments.empty? %>
  <p class="no-comments">Aucun commentaire pour ce potin.</p>
<% else %>
  <ul class="comment-list">
    <% comments.each do |comment| %>
      <li><%= comment.content %></li>
    <% end %>
  </ul>
<% end %>

<form action="/gossips/<%= gossip.id %>/comments" method="post" class="comment-form">
  <label for="comment_content">Ajouter un commentaire :</label><br/>
  <textarea name="comment_content" id="comment_content" rows="3" placeholder="Écris quelque chose de croustillant..."></textarea><br/>
  <input type="submit" value="Ajouter le commentaire" class="btn">
</form>

<style>
  body {
    font-family: Arial, sans-serif;
    margin: 2em;
    background-color: #f7f7f7;
    color: #333;
  }

  h1 {
    color: #2a7ae2;
  }

  .gossip-content {
    font-size: 1.2em;
    margin-bottom: 1em;
  }

  .actions {
    margin-bottom: 1.5em;
  }

  .actions form {
    display: inline;
  }

  .btn {
    display: inline-block;
    padding: 0.5em 1em;
    border-radius: 6px;
    background-color: #2a7ae2;
    color: white;
    text-decoration: none;
    margin-right: 0.5em;
    border: none;
    cursor: pointer;
    font-weight: bold;
  }

  .btn:hover {
    background-color: #1e5cb8;
  }

  .delete-btn {
    background-color: #e22a2a;
  }

  .delete-btn:hover {
    background-color: #b81e1e;
  }

  .comment-list {
    list-style-type: disc;
    padding-left: 1.5em;
    margin-bottom: 1em;
  }

  .comment-form textarea {
    width: 100%;
    padding: 0.5em;
    border-radius: 4px;
    border: 1px solid #ccc;
    font-size: 1em;
    margin-bottom: 1em;
    resize: vertical;
  }

  .no-comments {
    font-style: italic;
    color: #666;
  }
</style>
